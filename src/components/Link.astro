---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {
  href: string;
  external?: boolean;
}

const { href, external, ...attributes } = Astro.props;
---

<a
  href={href}
  {...external && { rel: "noopener noreferrer", target: "_blank" }}
  {...attributes}
  class="font-medium"><slot /></a
>
